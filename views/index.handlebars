<div class="container">
    <div class="wrapper">
        <div class="row justify-content-center">
            {{#each articles}}
            <div class="card">

                <div class="card-body">
                    <a href="{{link}}" target="_blank">
                        <h5 class="card-title">{{title}}</h5>
                    </a>
                    <p class="card-text">
                        {{summary}}
                    </p>
                    {{#unless saved}}
                    <button class="btn btn-secondary btn-sm save" data-id="{{id}}">Save Article</button>
                    {{/unless}}
                    {{#if saved}}
                    <p class="saved-article">This article is saved!</p>
                    {{/if}}
                    <hr class="my-4">
                    {{#if note}}
                    <p><strong>Comments:</strong></p>
                    {{/if}}
                    {{#each note}}
                    <ul>
                        <li>
                            <p>{{body}}</p>
                        </li>
                    </ul>
                    <button type="button" class="delComment btn btn-warning btn-sm" data-id="{{id}}">Delete
                        comment</button>
                    <hr class="my-4">
                    {{/each}}
                    <form class="comment" data-id="{{id}}">
                        <div class="form-group">
                            <label for="note">Add a comment</label>
                            <textarea class="form-control" class="body" required></textarea>
                        </div>
                        <button type="submit" class="btn btn-secondary btn-sm">Submit</button>
                    </form>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</div>